import React from 'react';
import style from './Tabs.module.css';

const Tabs = props => (
    <div className={style['tabs-container']}>
        <p className={style['tab']}>Conversaciónes</p>
    </div>
);

export default Tabs;
